server:
  port: 8760
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8760/eureka-server      #配置服务实例名称
    fetch-registry: false   #不获取服务列表
    register-with-eureka: false     #不向注册中心注册
spring:
  application:
    name: eureka-server     #指定服务实例名称